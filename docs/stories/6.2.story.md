# Story 6.2: SQL Builders и Fluent API

## Status
To Do

## Story
**As a** тест-разработчик,
**I want** fluent SQL-билдеры для SELECT, INSERT, UPDATE, DELETE,
**so that** я могу строить запросы типобезопасно и читаемо без написания сырого SQL.

## Acceptance Criteria
1. Реализован класс `SQL` как точка входа для создания билдеров.
2. Реализованы `SelectBuilder` и `ExecutableSelectBuilder` с поддержкой JOIN, WHERE (включая `whereIn`, `whereNull`, `whereIf`), GROUP BY, ORDER BY, LIMIT/OFFSET.
3. Реализованы `InsertBuilder` и `ExecutableInsertBuilder` с поддержкой `returningKeys()`.
4. Реализованы `UpdateBuilder` и `ExecutableUpdateBuilder` с защитой от UPDATE без WHERE.
5. Реализованы `DeleteBuilder` и `ExecutableDeleteBuilder` с защитой от DELETE без WHERE (требуется явный вызов `all()`).
6. Реализованы `BatchInsertBuilder` и `RawQueryBuilder` для batch-операций и сложных запросов.
7. Билдеры НЕ потокобезопасны — документация явно указывает создавать новый экземпляр в каждом потоке.
8. Unit-тесты для всех билдеров.

## Tasks / Subtasks
- [ ] Task 1: Реализовать класс SQL (AC: 1)
  - [ ] Subtask 1.1: Создать класс `SQL` с приватным конструктором
  - [ ] Subtask 1.2: Добавить статические методы `select()`, `selectAll()`, `insertInto()`, `update()`, `deleteFrom()`, `raw()`
- [ ] Task 2: Реализовать SelectBuilder (AC: 2, 7)
  - [ ] Subtask 2.1: Создать базовый `SelectBuilder` с columns и table
  - [ ] Subtask 2.2: Реализовать методы `from()`, `join()`, `leftJoin()`
  - [ ] Subtask 2.3: Реализовать методы `where()`, `whereIn()`, `whereNull()`, `whereNotNull()`, `whereRaw()`, `whereIf()`
  - [ ] Subtask 2.4: Реализовать методы `groupBy()`, `orderBy()`, `limit()`, `offset()`
  - [ ] Subtask 2.5: Реализовать `build()` для сборки Query
  - [ ] Subtask 2.6: Добавить Javadoc с предупреждением о потоконебезопасности
- [ ] Task 3: Реализовать ExecutableSelectBuilder (AC: 2)
  - [ ] Subtask 3.1: Создать `ExecutableSelectBuilder` extends `SelectBuilder`
  - [ ] Subtask 3.2: Добавить методы выполнения: `toList()`, `first()`, `single()`, `scalar()`, `count()`, `exists()`
- [ ] Task 4: Реализовать InsertBuilder (AC: 3)
  - [ ] Subtask 4.1: Создать `InsertBuilder` с table и values Map
  - [ ] Subtask 4.2: Реализовать методы `value()`, `values()`, `returningKeys()`
  - [ ] Subtask 4.3: Реализовать `build()` для сборки INSERT запроса
- [ ] Task 5: Реализовать ExecutableInsertBuilder (AC: 3)
  - [ ] Subtask 5.1: Создать `ExecutableInsertBuilder` extends `InsertBuilder`
  - [ ] Subtask 5.2: Добавить методы `execute()` и `executeAndGetKey()`
- [ ] Task 6: Реализовать UpdateBuilder (AC: 4)
  - [ ] Subtask 6.1: Создать `UpdateBuilder` с table, setValues и whereClauses
  - [ ] Subtask 6.2: Реализовать методы `set()`, `setAll()`, `where()`, `whereIn()`
  - [ ] Subtask 6.3: В `build()` выбрасывать исключение если нет WHERE условий
- [ ] Task 7: Реализовать ExecutableUpdateBuilder (AC: 4)
  - [ ] Subtask 7.1: Создать `ExecutableUpdateBuilder` extends `UpdateBuilder`
  - [ ] Subtask 7.2: Добавить метод `execute()`
- [ ] Task 8: Реализовать DeleteBuilder (AC: 5)
  - [ ] Subtask 8.1: Создать `DeleteBuilder` с table и whereClauses
  - [ ] Subtask 8.2: Реализовать методы `where()`, `whereIn()`, `all()`
  - [ ] Subtask 8.3: В `build()` выбрасывать исключение если нет WHERE и не вызван `all()`
- [ ] Task 9: Реализовать ExecutableDeleteBuilder (AC: 5)
  - [ ] Subtask 9.1: Создать `ExecutableDeleteBuilder` extends `DeleteBuilder`
  - [ ] Subtask 9.2: Добавить метод `execute()`
- [ ] Task 10: Реализовать BatchInsertBuilder (AC: 6)
  - [ ] Subtask 10.1: Создать `BatchInsertBuilder` с columns и rows
  - [ ] Subtask 10.2: Реализовать методы `columns()`, `row()`, `rows()`
  - [ ] Subtask 10.3: Добавить валидацию соответствия количества columns и values
- [ ] Task 11: Реализовать RawQueryBuilder (AC: 6)
  - [ ] Subtask 11.1: Создать `RawQueryBuilder` для произвольного SQL
  - [ ] Subtask 11.2: Реализовать методы `param()`, `params()`, `returningKeys()`
- [ ] Task 12: Написать unit-тесты (AC: 8)
  - [ ] Subtask 12.1: Тесты для SelectBuilder: проверка генерируемого SQL
  - [ ] Subtask 12.2: Тесты для InsertBuilder: проверка генерируемого SQL
  - [ ] Subtask 12.3: Тесты для UpdateBuilder: проверка защиты от UPDATE без WHERE
  - [ ] Subtask 12.4: Тесты для DeleteBuilder: проверка защиты от DELETE без WHERE
  - [ ] Subtask 12.5: Тесты для BatchInsertBuilder и RawQueryBuilder

## Dev Notes

### Архитектурный дизайн
[Source: architecture/hex-db-design-updated.md]
Полная спецификация всех билдеров с примерами использования описана в дизайн-документе, секция "2. Builders (пакет `builders`)".

### Ключевые решения
- **Защита от случайных операций**: UpdateBuilder и DeleteBuilder требуют WHERE или явный вызов `all()`.
- **Генерация параметров**: Именованные параметры генерируются автоматически для предотвращения SQL injection.
- **Условные WHERE**: Метод `whereIf()` позволяет добавлять условия динамически.

### Структура пакетов
```
hex-core-db/src/main/java/com/company/hex/db/builders/
├── SQL.java
├── QueryBuilder.java           ← интерфейс
├── SelectBuilder.java
├── ExecutableSelectBuilder.java
├── InsertBuilder.java
├── ExecutableInsertBuilder.java
├── UpdateBuilder.java
├── ExecutableUpdateBuilder.java
├── DeleteBuilder.java
├── ExecutableDeleteBuilder.java
├── BatchInsertBuilder.java
├── ExecutableBatchInsertBuilder.java
└── RawQueryBuilder.java
```