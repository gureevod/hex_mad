# Story 6.3: Db API, Конфигурация и Транзакции

## Status
To Do

## Story
**As a** тест-разработчик,
**I want** единую точку входа `Db` с простой конфигурацией и поддержкой транзакций,
**so that** я могу настроить подключение в одну строку и использовать транзакции для изоляции тестовых данных.

## Acceptance Criteria
1. Реализован класс `Db` как статическая точка входа с методами `configure()`, `select()`, `insertInto()`, `update()`, `deleteFrom()`, `raw()`.
2. Реализован `DbInstance` для поддержки multi-datasource сценариев (`Db.register()`, `Db.use()`).
3. Реализован `DbConfig` с builder API для простой и продвинутой настройки HikariCP.
4. Поддержка транзакций через `Db.transaction(Supplier<T>)` с ThreadLocal-изоляцией.
5. Реализован `QueryInterceptor` и `LoggingInterceptor` для логирования SQL с параметрами.
6. Методы `executeScript()` и `fromResource()` для выполнения SQL из файлов.
7. Методы `shutdown()` и `shutdownAll()` для корректного закрытия пула.
8. Интеграционные тесты с H2/PostgreSQL in-memory.
9. README.md модуля на русском с примерами использования.

## Tasks / Subtasks
- [ ] Task 1: Реализовать DbConfig (AC: 3)
  - [ ] Subtask 1.1: Создать `DbConfig` с полями для DataSource, interceptor, logging
  - [ ] Subtask 1.2: Создать `DbConfig.Builder` с методом `jdbc(url, user, password)` для простой настройки
  - [ ] Subtask 1.3: Добавить метод `hikari(Consumer<HikariConfig>)` для тонкой настройки
  - [ ] Subtask 1.4: Добавить метод `dataSource(DataSource)` для внешнего DataSource
  - [ ] Subtask 1.5: Добавить методы `enableLogging()`, `interceptor()`
- [ ] Task 2: Реализовать DbInstance (AC: 2, 4)
  - [ ] Subtask 2.1: Создать `DbInstance` с DbConfig и QueryExecutor
  - [ ] Subtask 2.2: Добавить ThreadLocal<Connection> для транзакций
  - [ ] Subtask 2.3: Реализовать методы создания билдеров: `select()`, `insertInto()`, etc.
  - [ ] Subtask 2.4: Реализовать `transaction(Supplier<T>)` с commit/rollback
  - [ ] Subtask 2.5: Реализовать `executeScript(String)` и `executeScript(Path)`
  - [ ] Subtask 2.6: Реализовать `fromResource(String)` для загрузки SQL из classpath
  - [ ] Subtask 2.7: Реализовать `close()` для закрытия пула
- [ ] Task 3: Реализовать Db (AC: 1, 2, 7)
  - [ ] Subtask 3.1: Создать `Db` с AtomicReference<DbInstance> для default instance
  - [ ] Subtask 3.2: Создать ConcurrentHashMap для именованных instances
  - [ ] Subtask 3.3: Реализовать `configure(DataSource)` и `configure(DbConfig)`
  - [ ] Subtask 3.4: Реализовать `register(String, DbConfig)` и `use(String)`
  - [ ] Subtask 3.5: Делегировать все методы билдеров в default instance
  - [ ] Subtask 3.6: Реализовать `shutdown()` и `shutdownAll()`
- [ ] Task 4: Реализовать QueryInterceptor и LoggingInterceptor (AC: 5)
  - [ ] Subtask 4.1: Создать функциональный интерфейс `QueryInterceptor`
  - [ ] Subtask 4.2: Создать `LoggingInterceptor` с интерполяцией параметров в SQL
  - [ ] Subtask 4.3: Добавить поддержку маскирования чувствительных данных
- [ ] Task 5: Реализовать DbException (AC: 1)
  - [ ] Subtask 5.1: Создать `DbException` extends RuntimeException
  - [ ] Subtask 5.2: Добавить конструкторы с message и cause
- [ ] Task 6: Написать интеграционные тесты (AC: 8)
  - [ ] Subtask 6.1: Добавить H2 как test-scoped зависимость
  - [ ] Subtask 6.2: Тесты CRUD операций с H2 in-memory
  - [ ] Subtask 6.3: Тесты транзакций: commit и rollback
  - [ ] Subtask 6.4: Тесты executeScript() с SQL-файлами
  - [ ] Subtask 6.5: Тесты multi-datasource через register()/use()
  - [ ] Subtask 6.6: Тесты batch insert
- [ ] Task 7: Документация (AC: 9)
  - [ ] Subtask 7.1: Создать README.md модуля на русском
  - [ ] Subtask 7.2: Добавить примеры базового использования
  - [ ] Subtask 7.3: Добавить примеры настройки пула и транзакций
  - [ ] Subtask 7.4: Добавить примеры multi-datasource

## Dev Notes

### Архитектурный дизайн
[Source: architecture/hex-db-design-updated.md]
Полная спецификация классов Db, DbInstance, DbConfig описана в дизайн-документе, секции "4. API (корень пакета)" и "5. Интерцептор".

### Примеры использования
```java
// Простая настройка
Db.configure(DbConfig.builder()
    .jdbc("jdbc:postgresql://localhost:5432/test", "user", "pass")
    .build());

// Тонкая настройка пула
Db.configure(DbConfig.builder()
    .hikari(config -> {
        config.setJdbcUrl("jdbc:postgresql://localhost:5432/test");
        config.setUsername("user");
        config.setPassword("pass");
        config.setMaximumPoolSize(10);
        config.setConnectionTimeout(5000);
    })
    .enableLogging()
    .build());

// Использование
List<Map<String, Object>> users = Db.selectAll()
    .from("users")
    .where("active", true)
    .toList();

// Транзакция
Db.transaction(() -> {
    Db.insertInto("orders").value("user_id", userId).execute();
    Db.update("users").set("orders_count", count + 1).where("id", userId).execute();
    return null;
});

// Multi-DataSource
Db.register("reporting", reportingConfig);
List<Map<String, Object>> stats = Db.use("reporting")
    .selectAll()
    .from("daily_stats")
    .toList();
```

### Структура пакетов (финальная)
```
hex-core-db/src/main/java/com/company/hex/db/
├── core/
│   ├── Row.java
│   ├── RowMapper.java
│   ├── Query.java
│   ├── QueryResult.java
│   ├── QueryExecutor.java
│   └── QueryInterceptor.java
├── builders/
│   ├── SQL.java
│   ├── SelectBuilder.java
│   ├── ExecutableSelectBuilder.java
│   ├── InsertBuilder.java
│   ├── ExecutableInsertBuilder.java
│   ├── UpdateBuilder.java
│   ├── ExecutableUpdateBuilder.java
│   ├── DeleteBuilder.java
│   ├── ExecutableDeleteBuilder.java
│   ├── BatchInsertBuilder.java
│   ├── ExecutableBatchInsertBuilder.java
│   └── RawQueryBuilder.java
├── executor/
│   ├── DefaultQueryExecutor.java
│   ├── SelectQueryResult.java
│   ├── ModificationQueryResult.java
│   └── MapRow.java
├── interceptors/
│   └── LoggingInterceptor.java
├── Db.java
├── DbInstance.java
├── DbConfig.java
└── DbException.java
```